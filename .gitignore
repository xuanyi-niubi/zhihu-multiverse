# dependencies
node_modules/
.pnp
.pnp.js

# next.js
.next/
out/
next-env.d.ts

# production
build/
dist/

# misc
.DS_Store
*.pem
Thumbs.db

# debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# env
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
# 生产密钥（部署时人工创建，绝不入库；.env.production.example 保留）
.env.production

# typescript
*.tsbuildinfo

# vercel
.vercel

# 本机 agent 摘要（含本机绝对路径侦察结果，不进公开仓库）
AGENTS.md

# 官方文档副本与个人构思底稿（不属于产品代码）
黑客松规则.txt
草稿*.txt

# ---------------------------------------------------------------------------
# 开发辅助物（本机用，不属于产品代码）
#
# 判据：这个目录/文件**只在开发时对本机有意义**，克隆到别处也无法使用，
# 或者每次运行都会产生不同的内容。三类：
#
#   1. agent 工作目录 —— 会话记忆、临时截图、素材转换中间产物。
#      这些东西换台机器就没用了，而且包含大量二进制（PNG/DB）。
#   2. 本地代码索引 —— 由工具扫描生成，体积大、可重建，路径是绝对路径。
#   3. 设计预览页 —— 开发时用来单独看某个组件的静态 HTML，
#      不是产品的一部分（产品页面在 src/app/ 下）。
#
# ⚠️ 用 `/` 锚定顶层。裸写 `.workbuddy/` 会匹配任意层级的同名目录 ——
# 与下面 /data/ 的坑同源（那边已踩过一次：裸 data/ 把 src/data/ 一起忽略，
# 新数据文件静默不入库，只在部署构建时才炸）。
# ---------------------------------------------------------------------------

# agent 工作目录：会话记忆 / 临时截图 / 素材转换中间产物
/.workbuddy/

# 本地代码图 / Understand Anything 索引（本地生成的体积产物，不入库）
/.codegraph/
/.ua/

# Yan Agent 会话快照 / 本地运行时状态（可能含工具输出，不入库）
/.yanagent/

# 设计预览页（开发时单独看组件用，非产品页面）
/design-system-preview.html
/game-design-preview.html

# 运行时数据（账号记忆等本地落盘内容，不入库）
# ⚠️ 必须写成 /data/：裸 data/ 会匹配任意层级的同名目录，
# 把 src/data/（剧本与角色数据）一起忽略 —— 新加的数据文件会静默不入库，
# 而且本地测试照样通过，只在部署构建时才炸。
/data/

# 中间产物与验证残留（同上，一律锚定顶层）
/work/
/work-verify/
/exports/

# 私有运维记录（部署 runbook / 访问入口 / 待办 / 备案与排查笔记）——不进公开仓库
/.private/

# 参赛材料与个人底稿 —— 本人自用，不进公开仓库。
#
# 这类文件的共同点是「为本人准备 / 本人查看」：参赛计划书要提交给主办方、
# 演示口播与交付记录是自己的工作稿。它们不是产品代码，公开也没有意义，
# 而其中可能含未定稿的表述与内部信息，所以一律本地保留。
/产品说明计划书.md
/docs/

# 测试统计生成器的中间产物（只保留 scripts/test-stats.json）
.test-stats-raw.json
