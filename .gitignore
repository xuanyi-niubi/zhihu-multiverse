# dependencies
node_modules/
.pnp
.pnp.js

# next.js
.next/
out/
next-env.d.ts

# production
build/
dist/

# misc
.DS_Store
*.pem
Thumbs.db

# debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# env
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
# 生产密钥（部署时人工创建，绝不入库；.env.production.example 保留）
.env.production

# typescript
*.tsbuildinfo

# vercel
.vercel

# 本机 agent 摘要（含本机绝对路径侦察结果，不进公开仓库）
AGENTS.md

# 官方文档副本与个人构思底稿（不属于产品代码）
黑客松规则.txt
草稿*.txt

# 运行时数据（账号记忆等本地落盘内容，不入库）
# ⚠️ 必须写成 /data/：裸 data/ 会匹配任意层级的同名目录，
# 把 src/data/（剧本与角色数据）一起忽略 —— 新加的数据文件会静默不入库，
# 而且本地测试照样通过，只在部署构建时才炸。
/data/

# 中间产物与验证残留（同上，一律锚定顶层）
/work/
/work-verify/
/exports/

# 私有运维记录（部署 runbook / 访问入口 / 待办 / 备案与排查笔记）——不进公开仓库
.private/

# 测试统计生成器的中间产物（只保留 docs/test-stats.json）
.test-stats-raw.json

# 本地代码图 / Understand Anything 索引（本地生成的体积产物，不入库）
.codegraph/
.ua/

# Yan Agent 会话快照 / 本地运行时状态（可能含工具输出，不入库）
.yanagent/
