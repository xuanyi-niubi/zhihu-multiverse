# 知乎平行宇宙 · 冲奖迭代方案

> 版本：2026-09-12 · 面向「知乎黑客松 2026 校园新锐季」初审（9/15 10:00 截止）与决赛路演（9/19）
>
> **本文件是行动方案，不是设计文档。** 设计事实源仍是 `DESIGN.md`；
> 本文件只回答一个问题：**在剩下的时间里，改什么能让这个项目拿奖。**
>
> 所有结论都标了代码位置或文档出处。没有出处的判断一律标「推断」。

---

## 0. 结论先行

这个项目的**工程质量已经超过黑客松平均水位很多**（1101 测试全绿、五层容错、确定性重放、零运行时依赖）。
它现在的风险**不在能力，而在暴露**：

| | 事实 | 出处 |
|---|---|---|
| 内核 | 经验引擎（逐字校验 / 强制反例 / unknown 一等公民）已跑通 | `src/features/experience/`，`tests/fullPersonalizedRun.test.ts` |
| ⚠️ 入口 | **首页三个按钮没有任何一个能走到新主链** | `src/app/page.tsx:288` `startCustomRun` → `launch(pickScenarioId())`（旧路径） |
| ⚠️ 衔接 | `/session/[id]` 页**没有**「进入世界」按钮，`prepare-world` 前端零调用 | `src/app/session/[id]/page.tsx` 全文无 `prepare-world` / `play?session` |
| ⚠️ 线上 | 线上四个凭证全空，评委看到的是 DEMO 降级版 | `.private/deploy/DEPLOYED.md` 待办 |

**一句话**：你 9/12 提交的 6 个 commit（经验引擎全链路）在评审视角里**等于不存在**，
因为评委会点首页最大的那个「投币开始推演」，而它走的是预置剧本。

**因此本方案的第一优先级不是加功能，是接通已有功能。**

---

## 1. 评分账本：先算清楚分在哪

初审与路演权重不同（`黑客松规则.txt` / 开发者手册）：

| 维度 | 初审 | 路演 | 现状 | 本方案动作 |
|---|---|---|---|---|
| AI 场景价值 | **40%** | 35% | 内核强，**入口不通** | P0-1 接通主链 |
| 创新度 | 25% | 25% | 经验解锁 + 世界线翻转 | P0-3 让纪律可见 |
| 完成度 | 25% | 25% | 很强（已部署 + 全绿） | P0-2 线上配 key |
| 产品体验与设计感 | 10% | 8% | 规范齐全 | 维持，不新增 |
| 计划书与演示 | — | 7% | 计划书已交，**演示脚本已过期** | P1-1 重写动线 |

### 1.1 必须承认的投入失衡

1101 个测试、五层容错、重放 100 次逐字节一致——这些**全部挤在「完成度 25%」这一格**，
而且**评委不看测试**。

`DESIGN.md §7.6` 里你给自己写过纪律：「每增加一个功能前先问三句话，两个否就不要做」。
同一把尺子现在该量的是：**再加一个测试，能换到分吗？**

> **本方案不新增任何测试驱动的机制**，只做三件事：接通、暴露、去伪。

---

## 2. P0：必须在提交前完成（决定 40% 那一格是否成立）

### P0-1 · 接通新主链 UI 入口 ★最高优先

**问题**：新主链 `输入 → 澄清 → prepare-world → /play?session=` 只能用 HTTP 调，UI 走不到。

`smoke-main-path.mjs:257` 只断言了 `GET /play?session=` 返回 200，
**没有断言有人能点到那个 URL** —— 这就是它全绿却仍然失联的原因。

**改动清单**：

| 文件 | 改动 |
|---|---|
| `src/app/session/[id]/page.tsx` | `status === 'ready_to_play'` 时渲染主按钮「进入这个世界」→ `router.push(/play?session=${id})`；澄清答完后渲染「生成我的世界」→ `act({action:'prepare-world'})` |
| `src/app/page.tsx` | 主按钮「投币开始推演」改走新主链：`POST /api/sessions` → 跳 `/session/[id]`；旧的 `pickScenarioId` 路径降级为「跳过澄清，直接推演」次级按钮 |
| `scripts/smoke-main-path.mjs` | 新增断言：`/session/[id]` 的 HTML 里必须含 `prepare-world` 触发点与 `/play?session=` 链接（防止入口再次失联） |

**验收**：从首页输入一句陌生困惑，**全程只用鼠标**能走到 `/play?session=` 并看到【经验解锁】徽标。

**为什么这是最高优先**：它把 40% 权重那一格从「评委看不到」变成「评委第一眼就看到」，
且**不新增任何机制**，只是把已经写好并测过的东西连起来。

---

### P0-2 · 线上配 key，别让评委看 DEMO 版

**问题**：`.private/deploy/DEPLOYED.md` 待办显示线上 `DM_API_KEY` / `ZHIHU_ACCESS_SECRET` /
`ZHIHU_OAUTH_APP_ID` / `ZHIHU_OAUTH_APP_KEY` **四个全空**，而本地 `.env.local` 已配好前两个。

**动作**：

1. 把本地已验证可用的 `DM_API_KEY` + `ZHIHU_ACCESS_SECRET` 写入服务器 `.env.production`，
   `docker compose up -d` 重启（**单实例硬约束不变**）。
2. **9/13 10:00 提交入口一开**，立刻走提交页拿官方分配的 `App_ID` / `App_KEY`
   —— 流程文档明确写了这两个由平台在提交页分配，**不需要自己去开放平台申请**
   （这条一直挂在你的部署待办里，其实走提交页就能拿到）。
3. **第一时间验证带 `:8443` 端口的回调地址平台是否接受**。

> ⚠️ **这是当前最大的未知风险**：你的正式入口是 `https://zhihu.xuanyi888.cloud:8443`
> （443 被运营商 SNI 级拦截，证据见 `DEPLOYED.md` 公网诊断一节）。
> 若平台不接受非标端口回调，OAuth 登录链路直接走不通，而
> **「登录人数是最佳人气奖评定条件之一」**。
> 必须 9/13 当天验证，不能等到 9/14 晚上。

**兜底**：若平台拒绝带端口回调，按 `DEPLOYED.md` 已列的三条路走
①借用已备案域名（小时级，最快）②迁香港轻量（半天）③ICP 备案（根治但 1–20 工作日，来不及）。
**选 ①**。

---

### P0-3 · 把「AI 不许改一个字」变成看得见的 3 秒钟

**问题**：这个项目真正不可替代的东西是三条纪律，但**界面上没有一个可视化瞬间**：

| 纪律 | 落点 | 现在评委能看到吗 |
|---|---|---|
| `exactQuote` 必须是原文逐字子串，改一字即丢 | `features/experience/validate.ts` | ❌ 看不到 |
| 强制检索反例（`counterexample` 硬性） | `features/experience/queryPlan.ts:108` | ❌ 看不到 |
| `unknown` 是一等公民，绝不出匹配度 | `features/experience/compare.ts` | 部分可见 |

`queryPlan.ts` 的注释写得非常好——
「只搜支持用户原本想法的内容，这个产品就退化成了一个讨好的搜索框」
—— **但这句话只有读代码的人才看得到。**

**改动**：复用已有的 `SourceBadge.tsx`（228 行）与 `EvidenceDrawer`，
给经验片段加一个可点徽标，点开显示：

```text
这段话逐字来自原文 —— AI 只负责挑选，不负责改写
原文：…（高亮命中区间）…
校验：exactQuote ∈ source.quote ✓
@答主 · 赞同 9,421 · 抓取于 2026-09-12 · [打开原回答 ↗]
```

并在路径卡上标注检索意图来源：`相似经历` / `另一种走法` / **`反例（我们主动去找的）`**。

**这是纯展示层工作，不改任何机制**，但它把「护城河」从代码注释搬到了评委眼前。
现有数据已经足够：60 条真实来源全部 `status: verified`
（实测 `src/data/zhihuSources.generated.json`，抓取于 2026-09-12T00:51Z）。

---

### P0-4 · 去伪：删掉没用上的猜测

**主动暴露一个没用上的猜测，是在给自己扣完成度分。**

| 项 | 现状 | 动作 |
|---|---|---|
| `ZHIHU_STORY_PATH` | 路径是猜的，README「已知限制」主动承认「故事接口路径待确认」 | **删掉配置与该条限制**（理由见 §4） |
| 搜索配额口径 | 代码注释与 README 写「1000 次/天」 | 官方最新文档是 **5000 次/天**，修正为 5000 |
| `MAX_SEARCH_REQUESTS = 4` | 按 1000/天 做的保守预算 | 放开到 6–8，直接提升经验丰富度 |
| 知乎直答 | README 写「已从模型链路移除」 | **保持移除**（不支持结构化输出，这个判断是对的，不要翻案） |

配额修正的位置：`src/core/zhihu/client.ts:9`、`src/core/zhihu/query.ts:13`、
`README.md` 环境变量表、`features/experience/queryPlan.ts:31`。

---

## 3. P1：提交后到路演前（9/15 → 9/19）

### P1-1 · 重写 3 分钟演示动线（`DESIGN.md §7.10` 已过期）

**问题**：现有逐秒脚本讲的是**旧的证据网格 + 双牌对比路线**，
与你 9/12 落地的新主链已经不一致。照现稿演示，等于当着评委的面展示旧版本。

**新动线建议**（3 分钟，Demo 实操为主）：

| 时间 | 画面 | 台词要点 |
|---|---|---|
| 0:00–0:20 | 首页输入一句**评委现场给的**陌生困惑 | 我们不做告诉年轻人「你该怎么选」的 AI。同一个选择，对不同条件的人答案完全不同 |
| 0:20–0:50 | 0～2 条动态澄清 | 注意它**没问我已经说过的事**——问的每一句都写明「答了会改变什么」 |
| 0:50–1:30 | prepare-world 检索过程 + 路径卡 | 三类检索是**强制**的：相似的人、另一种走法、**以及反例**。我们主动去找「后悔的人」，否则这就是个讨好你的搜索框 |
| 1:30–2:10 | 点开逐字徽标（P0-3 的产物） | 这段话是**逐字**的。AI 只有挑选权，改一个字就被丢掉——这条有测试钉着 |
| 2:10–2:40 | 进入 `/play?session=`，撞到【经验解锁】 | 一条真实经验，解锁了一个此前**不存在**的选项。你可以选择不参考它 |
| 2:40–3:00 | 终局 + unknown | 我们不给成功概率。这一项我们不知道，就写「还不知道」 |

**两个必须预备的问答**（沿用并更新）：

- **Q：和 AI 职业规划师有什么区别？**
  规划师给你判断，搜索给你链接。我们给你**一个可证伪的假设 + 从你自己条件算出的边界**，
  而且**七天后会回来收结果**。
- **Q：数据是真的吗？**
  三件事当场可验：① 点开原回答链接；② 逐字校验徽标；
  ③ 60 条来源是 `npm run sync:zhihu` 官方接口真实抓取落盘的，仓库里谁都能核对。

**演示纪律**（沿用 `§7.10` 血泪清单，仍然有效）：不现场贴 key、不手打长句、不等动画播完才说话。

---

### P1-2 · 演示视频（选交加分项）

规则写明「可用于平台展示、校园宣传推广」，对**人气奖**也有帮助。
按 P1-1 的动线录一条 2–3 分钟无解说版 + 带解说版。

> 注：当前环境 HyperFrames 视频生成未安装，需手工录屏。

---

### P1-3 · 人气奖的时间差（与代码质量无关，但是真金）

- 统计区间：**9.13–9.23 的作品 & 想法点赞量 + 使用量 + 评论量**，2 支 × ¥2000。
- 建队时「参赛想法链接」必填，要绑话题 `#知乎黑客松2026` 并同步「黑客松脑洞补给站」圈子。
- 规则原文：「不必等作品完全完成，哪怕只有一个 idea，也建议尽早提交占位，提前积累人气」。

**这是纯时间差收益，越早发越好。**

---

### P1-4 · 创作真实性佐证（防掉资格）

合规规范写明：**主办方有权核验创作真实性，无法提供佐证材料的队伍将取消参赛及评奖资格**。

你的佐证其实很强，但**目前对外不可见**：

| 材料 | 现状 |
|---|---|
| 完整 git 历史（16+ commit，含演进过程） | ✅ 但 `origin` 仓库需确认已推送 |
| `DESIGN.md §0.4` 演进史（网页→游戏→决策引擎三阶段） | ✅ 公开可见，**这是最好的佐证** |
| `.private/` 排查笔记、`AGENTS.md` | ❌ 都在 `.gitignore` 里，公开仓库看不到 |

**动作**：确认 `main` 已推送到 `github.com/xuanyi-niubi/zhihu-multiverse`，
并在提交表单的代码仓库字段填上（**选交加分项，且是评审重点加分项**）。

---

## 4. 明确不做（以及为什么）

范围收敛比范围扩张重要——这条是 `DESIGN.md §0.5` 用两版废弃方案换来的教训。

| 不做 | 理由 |
|---|---|
| **接「知乎故事」接口** | 盐言故事是**虚构小说**。接进来直接违反 `DESIGN.md §0.2` 原则 A「Evidence Before Imagination」，并让评委困惑「这游戏里的经验到底是真人的还是编的」。**你的差异化恰恰在于「不编」**——为蹭赛道范例而接虚构内容，是拿护城河换一个不需要的标签。主赛道范例里的「文字 AVG / 多结局 / AI NPC」你已全部满足 |
| 翻案接回知乎直答 | 只保证 `model/messages/stream` 三字段、不支持结构化输出，实测会把 JSON 指令写成 markdown 长文。移除是对的 |
| 再加测试 / 再加容错层 | 已在评分表里换不到分（§1.1） |
| 遗物套装共鸣、双骰检定、知识图谱复盘 | `README` 已如实列为未实现，**保持如实**，不要为了「看起来完整」赶工 |
| 新增 Agent / 向量数据库 / 社交系统 / 排行榜 | `DESIGN.md §7.6` 明确禁止清单，现在更不该破 |
| 热榜模块恢复 | `page.tsx` 已注释说明三条理由（不进机制 / 语义错配 / 占用最稀缺配额），判断正确 |

---

## 5. 时间线与验收门

| 时间 | 动作 | 验收 |
|---|---|---|
| **今晚 9/13 00:00 前** | ⚠️ 报名 + 建队截止（含身份证明、队员须先报名） | 队伍创建成功 + 想法已发布 |
| 9/13 10:00 | 提交入口开启：占位提交拿 `App_ID`/`App_KEY`；验证 `:8443` 回调 | `/api/oauth/status` 不再报凭证为空 |
| 9/13 白天 | **P0-1 接通主链** | 全程鼠标可达 `/play?session=`；smoke 新断言通过 |
| 9/13 晚 | P0-2 线上配 key + P0-4 去伪 | 线上引擎灯显示 FULL 而非 DEMO |
| 9/14 | **P0-3 逐字诚信可视化** | 评委可点开任一片段看到校验徽标与原文链接 |
| 9/14 晚 | `npm run verify` 全绿 + 线上回归 | typecheck + 1101 测试 + build + smoke 全过 |
| **9/15 10:00 前** | 提交（**不要卡 09:59**，初审是滚动打分，早交可能被更从容地看） | 四项材料齐全 |
| 9/15–9/17 | P1-1 演示动线 + P1-2 视频 | 3 分钟能讲完且不超时 |
| 9/19 下午 | 798 路演（3 分钟 Demo + 2 分钟 Q&A，最多 2 问） | — |

**每一步 P0 完成后都必须跑 `npm run verify`**（typecheck + 全部测试 + 构建 + 冒烟），
这是你自己定的提交前门槛，不要在冲刺期破它。

---

## 6. 一句话总结

> 这个项目不缺能力，缺的是**让评委在 3 分钟内撞上它的能力**。
>
> P0 三件事——**接通入口、配上线上 key、把「AI 不许改一个字」变成看得见的一屏**——
> 都不新增机制，只把已经做好的东西搬到评委眼前。
>
> 剩下的时间别再加功能了。**真实的人走过的路，是这个世界的规则**——
> 这句话已经写进代码了，现在要让它写进评委的记忆里。
