# 知乎平行宇宙 · 设计文档（唯一事实源）

> **本文件是全项目唯一的设计文档。** 产品定义、游戏机制、技术架构、视觉规范都在这里。
>
> **阅读顺序建议**：
> - 想了解「这作品是什么、解决什么问题」→ §0
> - 想了解「玩家在玩什么」→ §4–§7
> - 想了解「代码怎么长」→ §3 · §7
> - 想了解「界面怎么画」→ §8 起（第二部分）
>
> **原 `docs/` 目录已并入本文件。** 任何与代码冲突之处，以「代码 + 测试」为准，
> 并应回头修本文件 —— 文档漂移比没有文档更危险。

---

# 第一部分 · 产品、机制与架构

## 0. 产品定义

### 0.1 一句话与定位

**知乎平行宇宙是一款由知乎真实人生经验驱动的 AI 平行人生 Roguelike。**

玩家进入一个「平行版本的自己」，经历人生选择、资源消耗、临界事件和世界线分叉。
知乎提供真实世界里「别人已经走过的路」，AI 负责理解、抽取与叙事，
而最终世界线能否成立，**由确定性游戏规则裁决**。

**Slogan**：你改变的不是答案，而是答案成立的条件。

| 层 | 内容 |
|---|---|
| 表层：好玩 | 3 分钟进入一个人生裂缝，体验一个属于自己的平行宇宙 |
| 中层：独特 | 真实知乎经验不是装饰，而是游戏机制的数据来源 |
| 底层：有意义 | 不替玩家决定人生，而是找出「哪个变量一旦改变，世界线就会翻转」 |

**赛道**：知乎黑客松 2026 校园新锐季 · 主赛道「跨次元游乐场」+ 副赛道「知识炼金场」。

### 0.2 四方分工与绝对原则

**知乎 = 现实世界**（真人经历、真实路径、真实代价、失败与回头成本）；
**AI = 解释世界**（理解玩家、拆解问题、结构化经历、生成场景与叙事）；
**游戏引擎 = 执行世界规则**（四轴、临界点、路径要求、世界线稳定性、结局裁决）；
**玩家 = 改变世界**（不是点「正确选项」，而是不断改变资源与条件）。

四条绝对原则，任何改动都不得违反：

- **A · Evidence Before Imagination** —— 先有证据，再有想象。AI 可自由叙事，但无证据内容不得进入胜负裁决。
- **B · AI 不决定胜负** —— 任何「可行 / 越界 / 未知」都必须来自纯函数游戏规则。
- **C · 未知就是未知** —— 无据字段保持 `null / unknown`，不为体验完整而补数字。
- **D · 随机决定遭遇，不决定真相** —— 随机只决定「遇到什么」，不决定真实路径是否可行。

### 0.3 当前实现状态

| 项 | 值 |
|---|---|
| 仓库 | https://github.com/xuanyi-niubi/zhihu-multiverse |
| 线上 Demo | https://zhihu.xuanyi888.cloud:8443 |
| 技术栈 | Next.js 14.2.15（App Router）+ React 18.3.1 + TypeScript 5.5.4 + Tailwind，**无其他第三方运行时依赖** |
| 质量门 | `tsc --noEmit` 通过 · `next build` 通过 · 测试全绿（唯一口径 `scripts/test-stats.json`，由 `npm run test:stats` 生成） |
| 真实数据 | 已跑 `npm run sync:zhihu`：**60 条真实来源**（20 锚点 / 5 Case），真答主 + 真赞同数 + 真链接 + 官方权威等级 |

**已落地**：证据网格（`/api/mesh`）、机制裁决与临界点、双牌对比（`/compare`）、
承诺回执闭环（`/commitment`）、四维结算、快慢双流模型路由、三种运行模式与降级链、
动态幕接线、常驻四轴 `AxisHUD`、三个黄金 Demo Case。

**已知数据质量边界（如实记录）**：`strength` 的**新鲜度因子恒为 1** ——
官方 `EditTime` 字段实测返回近期时间戳（与 `retrievedAt` 同量级），不像用户真实编辑时间。
未为了「看起来有区分度」而改公式，因为那会变成用假信号美化指标。

### 0.4 演进史：从「网页」到「游戏」（原 `design-evolution.md`）

本项目的形态经历过一次关键重构，记录在此以免重走回头路：

1. **第一阶段（网页）**：当成「AI 生成内容的展示页」—— 首页填表、生成一段文本、显示几个角标。
   问题：用户没有「在玩」的感觉，也没有任何东西值得第二次打开。
2. **第二阶段（游戏）**：确立「舞台优先」—— 主视觉留给「此刻谁在对你说话」，
   元信息退到 HUD 与抽屉。这一步产出了视觉规范（第二部分）与参数化 SVG 立绘引擎。
3. **第三阶段（决策引擎）**：发现「AI 讲故事」不构成差异化 —— 任何人都能做。
   于是把重心移到**确定性引擎**：掷骰子 → 机制裁决，固定分支 → 约束驱动，
   一次性清单 → 承诺回执。这一步产出了 §4–§7。

> **下一次如果有人想「再加一堆 Agent / 再加大量场景」，请先读 §7.6 的开发纪律。**

### 0.5 上一阶段的设计基线（原 `设计方案-最终版.md` 要点）

上一阶段解决的是「剧情被写死」——三项硬编码：

| 问题 | 原状 | 处置 |
|---|---|---|
| 回合数硬编码 | `turnIndex` 固定 1..4、难度按回合号查表 | 引入张力预算驱动的动态幕（`core/run/actEngine.ts` + `actRun.ts`） |
| 每回合永远 2 选项 | 「一稳一险」形状不变 | schema 与校验层同步放宽到 1..3 |
| 内容规模太小 | 场景骨架 3 个、遗物 6 件 | 扩到 12 个骨架、15 件遗物（每件带叙事钩子） |

**该阶段的教训直接写进了纪律**：范围收敛比范围扩张重要。
被废弃的两版方案共同问题是「提出叙事操作系统级重构，远超剩余时间预算，且未验证任何一行现有代码」。

---

## 1. 核心循环 Core Loop

```
人生裂缝 → 平行身份生成 → 获得初始四轴 → 剧情事件 → 玩家选择
   → 资源与状态变化 → 知乎证据约束进入游戏 → 临界事件 / Boss
   → 改变条件或退回世界线 → 世界线分叉 → 终局结算
   → 认知记忆 / 七天现实实验
```

**每一局目标时间 3–5 分钟**，且一局必须至少出现：

- 1 次明确属性变化；
- 1 次真实证据展示；
- 1 次临界点；
- 1 次世界线翻转或未翻转；
- 1 次终局假设。

---

## 2. 三种运行模式

模式**由能力推导，不由开关决定**（免得出现「配了 key 却还在演示模式」或反过来）。

| 模式 | 条件 | 能力 | 界面标记 |
|---|---|---|---|
| **FULL** | AI Key ✓ + 知乎 Key ✓ | 动态理解 + 实时检索 + 动态叙事 + 确定性裁决 | `AI ENGINE ● ONLINE` / `ZHIHU EVIDENCE ● ONLINE` |
| **AI** | 只有 AI Key | 动态叙事；**重大裁决只用离线规则与已验证数据** | `AI ● ONLINE` / `ZHIHU ○ OFFLINE` + 明确告知 |
| **DEMO** | 无 Key | 精调场景 + 落盘快照 + 确定性引擎，**100% 可演示** | 两个离线，如实说明不编造经历 |

`WORLD ENGINE` 永远 `● READY` —— 它是纯函数，不依赖任何外部服务。

**降级链**：`live → snapshot → offline`，任何外部 API 挂掉都不影响一局完成。
实现见 `core/run/runtimeMode.ts` 的 `fallbackChain()`，接口 `/api/health` 只回在线/离线，
**密钥永不出服务端**。

---

## 3. 产品整体架构

```
玩家输入 / 选择
      ↓
AI Query Planner（处境 / 目标 / 分歧 / 约束）
      ↓
知乎检索层（搜索 / 快照 / 路径样本）
      ↓
Path Extractor（自然语言经历 → 结构化路径卡）
      ↓
Evidence Mesh（真人路径聚类 / 成本画像 / 证据强度）
      ↓
Deterministic Decision Engine（四轴需求 / verdict / critical point）
      ↓
Game Runtime（事件 / 属性变化 / 世界线分叉 / Boss）
      ↓
AI Narrative Layer（把结果叙事化，**不修改结果**）
      ↓
玩家
```

---

## 4. 游戏核心机制

### 4.1 四轴系统（`core/decision/axis.ts`）

| 轴 | 含义 | 硬/软 | 上界从哪来 |
|---|---|---|---|
| `runway` 时间余量 | 还能不赚钱地撑几个月 | **硬** | 前人时间成本上沿 |
| `drawdown` 承压能力 | 能承受的亏损幅度（资金 / 机会成本 / 心理） | **硬** | 前人资金代价档位 |
| `reversibility` 回头空间 | 走错之后还能不能切回原路径 | 软 | 前人是否明说「回不来」 |
| `ally` 同行者 | 是否有伙伴 / 导师 / 家庭共同承担 | 软 | 前人经历里是否依赖同伴 |

**硬轴**越线即触发明确世界线危机；**软轴**改变代价与恢复速度，但不直接判死。

**两条不可违反的实现纪律**：
1. **缺项不产生需求** —— `null` / `unknown` 绝不被当成 0。把「不知道」当「不需要」
   是证据类产品最致命也最容易犯的错。
2. **跨轴比较用绝对缺口，不用区间占比** —— 12 个月的缺口和 15 点亏损必须能直接比较；
   用比例会让区间大的轴永远看起来更紧，从而把真正的瓶颈藏起来（这一条有测试锁死）。

### 4.2 机制裁决（`core/decision/verdict.ts`）

**不再用随机数决定人生真相。** 每条路线有硬轴要求，玩家条件是否满足决定结论：

| 结论 | 条件 |
|---|---|
| `viable` | 全部硬轴通过；给出余量与最紧的那条轴 |
| `breached` | 某条硬轴不足；**必须**给出越线的轴与超出量 |
| `unknown` | 证据不足（`thin`）→ **不裁决**，绝不猜 |

三条不变量（`tests/decisionVerdict.test.ts` 锁定）：
**I-A** 纯函数（同输入 20 次重放逐字节一致）· **I-B** 失败必须具体 · **I-C** 无据不裁决。

### 4.3 签名机制：临界点 Boss

`verdict` 返回 `breached` 时**不直接显示「失败」**，而是进入「临界事件」：

```
世界线稳定性下降
你还能坚持：3 个月      这条路径需要：9–12 个月      当前缺口：6 个月
```

> **你不是选错了。你只是撞上了现实的边界。**

玩家**不能花复活币**，必须改变真实条件（兼职换时间余量 / 找伙伴换 Ally /
降低目标改路径要求 / 用 Reversibility 退回原世界线）——
这些动作进入真实世界状态，而不是只改文案。

**判定标准重构**：赢 ≠ 走完四幕。四维认知结算（`core/decision/clarity.ts`）：
**清晰度**（能否说清自己在哪条轴上最紧）· **证据覆盖** · **代价认知**（预判 vs 真实）·
**可逆性管理**（有没有留退路）。

### 4.4 双宇宙对比（`core/decision/sandwich.ts` + `/compare`）

同一份证据网格 × 两组约束并排推演，**唯一变量是玩家自己的处境**。
拖动滑杆时两侧实时重算 —— **不发网络请求**，只调决策纯函数
（这是「裁决是纯函数」这个架构决定的兑现：滑杆反馈必须像在捏脸）。

当 A 从 `breached` 变成 `viable`，出现：

> **你刚刚没有改变选择。你改变的是选择成立的条件。**

### 4.5 随机性的位置

**删除**「随机决定人生真相」；**保留**随机事件：遇到贵人、项目失败、意外机会、
家庭事件、市场变化 —— 这些只修改 `WorldState`，最终能否扛住仍由确定性规则计算。
这样保留 Roguelike 味道，同时不破坏证据可信度。

---

## 5. 知乎证据系统

### 5.1 定位

**知乎不是资料引用，而是游戏世界规则的数据源。** 拿掉它，这仍是一款 AI 叙事游戏；
加入它，才成为一款由现实世界驱动的平行人生游戏。

### 5.2 Evidence Card（一条真实经历的结构化形态）

每一个关键数值都必须可点开：答主 · 年份 · 赞同数（**仅 verified**）· 原文短摘 ·
原回答链接 · 抓取时间 · 抽取字段 · 证据强度。

抽取的六要素**每一项都可能为 `null`**，为 `null` 时如实显示「未知」：
`from` 起点处境 · `move` 做了什么 · `duration` 花了多久 · `cost` 付出的代价 ·
`outcome` 结果（止于原文口径，不推广成规律）。

### 5.3 Evidence Mesh

分成 **7 条互斥路线**（`core/evidence/mesh.ts` 的 `PATH_ARCHETYPES`）：
脱产转型 / 在职转型 / 跨行换赛道 / 原地深耕 / 求稳路线 / 先缓一缓 / 平级跳板。
每条路线聚合**多条同向样本**（一条只是个人经历，多条才是一条前人路径），
并计算代价画像与证据强度。

**证据强度**（`core/evidence/strength.ts`）四因子，权重刻意让样本量主导：

| 因子 | 权重 | 说明 |
|---|---|---|
| 样本量 | 0.4 | 对数收敛；一个人说的不叫证据 |
| 权威度 | 0.25 | 平台给的 L1–L5 等级，不做二次解读 |
| 新鲜度 | 0.15 | 不衰减到 0（老经验有长尾价值） |
| 一致性 | 0.2 | 结果说法互相矛盾必须降权 |

**`strong` 必须同时满足强度 ≥0.6 且样本量 ≥3** —— 一个人说了不算。

### 5.4 强制规则（代码层执行，不是口号）

1. **只有 `verified` 才展示权威指标**（`features/run/knowledgeSource.ts` 的 `sourceBadgeLabel` 是唯一执行点）；
2. **`scripted` / demo 必须有明显标记**，且**永远进不了证据网格**（`snapshotToPathCards` 主动拒绝）；
3. **`thin` 不进入硬裁决**（`verdictFor` 返回 `unknown`）；
4. **`unknown` 不补全**（缺项保持 `null`，不被当成 0）。

### 5.5 三个黄金 Demo Case（真实快照聚合）

| Case | 玩家目标 | 抉择分野 |
|---|---|---|
| **大二的夏天**（主 Case） | 大二，基础一般，想直接参加比赛，但怕耽误课程 | 先补基础再参赛 vs 直接参赛边做边学 |
| **毕业前夜** | 应届毕业，纠结留在大城市还是回老家 | 留在大城市闯 vs 回老家考编稳住 |
| **辞职信** | 工作两年想转行 AI | 裸辞全力冲 vs 边工作边转型 |

**⚠️ 数据口径（必须知情）**：Case 是**真实检索结果的聚合视图，不是手写数据**。
检索词与路线归类由人工确认（同步脚本的 `TARGETS[].path` → 落盘 `anchorPaths`），
经历内容 100% 来自官方接口真实抓取。

> 之所以这样设计：如果手工编写「前人经历」，就违反了 §0.2 原则 C 与「demo 不可伪装 verified」。
> 两者不能同时满足时，选择**不编造**。「人工核验」落在「确认哪些议题值得探、
> 这些样本属于哪条路」，而不是「编造经历」。

**没有这一步会怎样（实测）**：真实语料的措辞不可预测 ——
「转行 AI」「留在大城市」不会命中任何路线关键词，60 条真实来源会全部变成孤儿卡。
因此 Mesh Builder **优先采用归档提示**，关键词匹配只作兜底。

---

## 6. AI 系统

### 6.1 AI 的五个职责

| 职责 | 说明 |
|---|---|
| A · Intent Planner | 自然语言 → 处境 / 目标 / 分歧 / 约束 |
| B · Retrieval Planner | 为知乎搜索生成多方向查询（处境 / 分歧 / 约束三向） |
| C · Path Extractor | 知乎回答 → 结构化路径卡（逐字段可空） |
| D · Narrative Director | 游戏事件与场景表达 |
| E · Memory Narrator | 读取确定性记忆记录，用自然语言把「过去」带入当前局 |

### 6.2 AI 不允许做的事

直接裁决成功 / 失败 · 生成伪造知乎来源 · 无证据生成关键数值 ·
修改 Decision Engine 返回值 · 将 `scripted` 内容伪装成 `verified`。

### 6.3 五层容错与快慢双流

**五层容错**（`core/dm/`）：Prompt 约束 → JSON 模式传输 → 容错解析 → 校验与钳制修复 → 编排兜底。
`/api/dm` 契约是**永远 HTTP 200 + 合法关卡**，前端不需要写错误分支。

**快慢双流**（`agents/tieredRouting.ts`）：
深流角色（情节导演 / 世界模拟 / 角色扮演）优先深档；快流角色（编排 / 记忆 / 风格）优先快档；
未配置分档时只注册一个 provider，路由自动退化 —— **零回归**。

> 比赛叙事不要强调「支持多少模型」，只强调：**AI 模型可替换，但游戏真相不由模型决定。**

### 6.4 统一协议层

`AI_BASE_URL` / `AI_API_KEY` / `AI_MODEL`（兼容旧 `DM_*`）+ `ZHIHU_ACCESS_SECRET`。
可在不改游戏业务逻辑的前提下替换模型提供商。

---

## 7. 技术架构

### 7.1 分层

```
┌─────────────────────────────────────────────────────────────┐
│ 表现层 src/app + src/components                              │
├─────────────────────────────────────────────────────────────┤
│ 决策层 src/core/decision   ★ 纯函数，无随机、无网络            │
│  axis / verdict / sandwich / clarity                         │
├─────────────────────────────────────────────────────────────┤
│ 证据层 src/core/evidence   ★ 确定性聚合，AI 只做抽取           │
│  plan / cards / mesh / strength / meshClient                 │
├─────────────────────────────────────────────────────────────┤
│ 知识层 src/core/zhihu（检索 + 配额缓存 + 快照）                │
│ AI 层 src/agents + src/core/dm（五层容错，职责收窄）           │
│ 记忆层 src/core/memory（账号隔离 + 原子写 + 真删）             │
└─────────────────────────────────────────────────────────────┘
```

### 7.2 接口契约

| 接口 | 契约 |
|---|---|
| `POST /api/dm` | 永远 200 + 合法 `ScenarioTurn`；`x-dm-source` 标来源 |
| `POST /api/mesh` | 永远 200 + 可用网格；`queryPlan / cards / mesh / strength / provenance` |
| `GET /api/health` | 只回运行模式与在线状态，**不含任何密钥** |
| `POST /api/commitment` | 未登录静默成功；`ok` 与 `persisted` 分开 |
| `POST /api/commitment/receipt` | 回执幂等；回执驱动下一局约束建议 |

### 7.3 确定性设计

- **随机源唯一**：`mulberry32`，由 `seed + checkId + turnIndex` 派生；
- **判定优先级**：天然 1 必败、天然 20 必成，高于数值比较；
- **数值钳制**：属性 0–100、DC 1–30、单项修正 ±20、总修正 ±60；
- **幕数口径唯一**：`core/run/actRun.ts` 的 `MAX_TURNS`（原四处不一致已统一）；
- **网格可哈希**：`meshHash` 是纯函数产物，与输入顺序无关，可进挑战链接。

### 7.4 三个运行模式的降级实现

见 §2。要点：`fallbackChain(mode)` 保证**每条链都以 `offline` 结尾**，
任何模式都不会无路可走（`tests/runtimeMode.test.ts` 锁定）。

### 7.5 记忆系统（只做 MVP）

**不做「聊天记忆」**。目标是一句话：**让玩家上一局留下的选择，进入下一局。**

三类记忆：**世界记忆**（scenarioId / choiceId / axisBefore / axisAfter / verdict / criticalPoint / ending）·
**认知记忆**（被明确生成的假设 + 支撑/推翻状态）· **现实回执**（七天实验的完成状态）。

比赛版边界：只实现最近 20 局、关键选择、临界点、假设、回执。
**不引入向量数据库与复杂长期 Agent Memory**，除非核心体验已全部完成。

### 7.6 开发纪律

**每增加一个功能前先问三句话**：

1. 它让「知乎真实人生经验」更不可替代了吗？
2. 它让玩家更像在「玩」，而不是在「看报告」吗？
3. 它能让评委在 3 分钟内记住吗？

**三个问题里两个答案为「否」，就不要做。**

**明确禁止现在做**：再新增一堆 Agent · 自建复杂向量数据库 · 重写整个技术栈 ·
追求大量新场景 · 做社交系统 · 做排行榜 · 做复杂账号体系 · 用随机数重新决定胜负。

### 7.7 埋点（最小集合）

`run_started` · `mesh_loaded` · `mesh_fallback` · `choice_made` · `critical_triggered` ·
`worldline_flipped` · `evidence_opened` · `commitment_created` · `run_finished`。

开发环境记录完整 debug；生产环境避免泄漏 Key 与原始敏感输入。

### 7.8 安全与密钥

**Key 永远只在服务端** · 不写 LocalStorage · 不通过 URL · 不进截图与日志 ·
客户端只接收健康状态 · Demo 设备使用单独 Key · 路演后立即轮换。
用户自由输入避免永久原文保存；记忆层尽量保存结构化事实而非整段私密文本。

### 7.9 部署注意事项（修改这些文件前必读）

**① 排除目录必须锚定根目录：`/data/` 而不是 `data/`**

`.gitignore` 与 `.dockerignore` 里裸写 `data/` 都会被当作**可匹配任意层级**的模式，
于是把 `src/data/`（剧本、角色、场景、真实知乎快照）一起排除。两种症状：

| 文件 | 症状 |
|---|---|
| `.gitignore` | 新加的数据文件静默不入库，本地测试照样通过，只在部署构建时才炸 |
| `.dockerignore` | `next build` 直接失败：`Module not found: Can't resolve '@/data/...'` |

**`rsync` 同理**：`--exclude 'data/'` 会跳过 `src/data/`；
部署时保护运行目录的 `data/` 必须写 `--exclude '/data/'`。
（写错时 `src/data/demoCases.ts` 不会进入运行目录，而部署脚本的
文件存在性检查只在 staging 上做，于是「检查通过但构建失败」。）

**② 部署脚本的核对必须在目标目录做，不能只在暂存目录做**

上面那个坑之所以能穿过检查，是因为检查跑在 staging（文件齐全），
而 rsync 的目标目录（文件缺失）。核对的目标目录与写入的目标目录必须一致。

**③ `.env.production` 不在仓库里**

它被 `.gitignore` 与 `.dockerignore` 双重排除（正确）。因此
**同步代码时绝不能让它被覆盖或删除**：rsync 必须 `--exclude '.env.production'`，
并在同步前 `cp -a` 备份。生产凭证只有服务器上那一份。

**④ 单实例是硬约束**（见 Dockerfile 顶部说明）

OAuth 会话存在 Node 进程内存里，多副本会让授权状态在实例间丢失。

### 7.10 路演脚本与镜头清单

> **对应规则**：3 分钟 Demo 实操 + 2 分钟 Q&A（最多 2 个问题）。
> 下表数字取自线上实例 `zhihu.xuanyi888.cloud:8443` 的实际测量。

#### 演示前 60 秒准备（务必照做）

| 步骤 | 动作 | 为什么 |
|---|---|---|
| 1 | 打开 `https://zhihu.xuanyi888.cloud:8443` ，**不要**现场输入 key | 站点默认 DEMO MODE：预置剧本 + 已落盘真实快照，零延迟零配额，不受网络抖动影响 |
| 2 | 确认引擎灯为 **DEMO**（不是 FULL） | 这是**主动选择**：DEMO 下所有内容可复现 |
| 3 | 浏览器缩放 100%，窗口 ≥1280px | 启动按钮在 1280×720 首屏内可见（已实测） |
| 4 | 预开第二个标签：`/compare?goal=大二，基础一般，想直接参加比赛，但怕耽误课程` | 那一页要现算，预开省 2 秒 |
| 5 | 关掉所有通知弹窗 | 翻车最多的地方 |

> **绝不现场贴 Key**。三个引擎灯就是这件事的替代方案。

#### 逐秒脚本

| 时间 | 画面 | 台词要点 |
|---|---|---|
| 0:00–0:20 | 首页「人生裂缝大厅」 | 我们不做一个告诉年轻人「你应该怎么选」的 AI。同一个选择，对不同资源、不同时间窗口的人，答案完全不同。所以我们做的是：**从知乎找到真实走过的路，把它变成游戏规则。** |
| 0:20–0:50 | 点《大二的夏天》→ 开证据网格 → 展开前人经历 | 目标不是 AI 编的：背后是 **60 条真实知乎来源**，带答主 / 年份 / 赞同数 / 原链接。注意标着 **UNKNOWN** 的字段 —— 原文没提到时间成本，我们就写「未知」，不补数字，**这条被测试锁着**。 |
| 0:50–1:30 | 选带风险的选项 → 骰子模态 | 普通 AI 文字游戏是「掷骰子大于难度就成功」，我们把它拆了。这个弹窗写的是 **REALITY CHECK**：成败来自**你的条件和这条路的需求** —— 它要 12 个月你有 6 个月，所以走不通。骰子只掷**遭遇**。**随机决定遭遇，不决定真相。**（说完停 1 秒） |
| 1:30–2:10 | 切对比页 → 拖约束滑杆触发翻转 | 同一个你、同一份证据，只是条件不同。**证据完全相同，唯一变量是你自己。** 调一下能撑的时间 —— 看：**结论当场翻转**。（触发演出）**世界线已翻转。你刚刚没有改变选择，你改变的是选择成立的条件。** |
| 2:10–2:35 | 临界点卡片 | 走不通时我们不显示「失败」，只指出：**这条路上你唯一的硬伤是可投入时间，其余约束你都扛得住。** 你不是选错了，你只是撞上了现实的边界。 |
| 2:35–3:00 | 勾选承诺 → 切 `/commitment` | 它把你认下的动作记下来，**七天后回来问你结果**，再把结果变成下一局的参数。它不是玩完就卸载的东西。 |

#### 两个必须预备的问答

**Q1：这和 AI 职业规划师 / AI 搜索有什么区别？**
> 规划师给你一个判断，AI 搜索给你一堆链接。我们给你**一个可证伪的假设，以及从你自己的约束算出来的边界**。区别在最后一步：**七天后我们会回来收结果** —— 而它们从不回收。

不要说「我们更懂用户」「我们有更好的模型」。要强调**闭环**与**可证伪**。

**Q2：数据是真的吗？会不会是预写的？**
> 三件事可当场验：① 证据卡有答主 / 年份 / 赞同数 / **原回答链接**，可以点开；② 证据不足的路线我们显示「证据不足」而不是编一条，**这条有测试锁着**；③ 演示数据是 `npm run sync:zhihu` 用官方接口真实抓取落盘的，仓库里那 60 条谁都能核对。

被追问「AI 会不会幻觉」时补一句：AI 可以讲故事，但**不能决定胜负**；
模型写进去的骰面、DC、成败会被结构性丢弃。

#### 备用镜头

| 情况 | 备用动作 |
|---|---|
| 时间多 20 秒 | 推演舱展示**命运事件卡**（「遇到什么」与「条件怎么变」分开写） |
| 对比页加载失败 | 用 `/compare?goal=大二想参加比赛`（短目标也能命中快照） |
| 评委想看技术 | 打开本文件 §4–§7（机制与架构）或 README 的测试清单 |
| 被问「为什么是游戏赛道」 | 指世界线母题：五个页面共用同一套视觉语言 |

#### 演示纪律（血泪清单）

1. **不要现场配 Key** —— DEMO MODE 已足够展示全部机制。
2. **不要手打长句** —— 要输入的都用预开 URL 参数。
3. **不要等动画播完才说话** —— Flip 演出 2.6 秒，正好覆盖一句台词。
4. **不要在对比页等网络** —— 它是本地纯函数重算。
5. **带着结论讲** —— 每段都以「所以这意味着什么」收尾。

> **只剩 10 秒时的一句话**：真实的人走过的路，是这个世界的规则。
> 你改变的不是答案，而是答案成立的条件。

---

# 第二部分 · 视觉与交互规范

> **编号说明**：本部分保留原有的 9 章编号（§1–§9），与第一部分的 §0–§7 是两套独立编号。
> 引用视觉规范时请写「第二部分 §4.3」这类带部分名的形式，避免与产品章节混淆。
> 本部分由 `web-design` 流程产出，代码中的颜色、字体、动效档位、组件状态均须回溯到本章。
> §4.6–§4.12 是本轮新增组件（四轴 HUD / 证据应力条 / 临界点 / 双牌对比 / 赛博契约 / 四维结算 / 引擎状态条）。

---

## 1. Visual Theme & Atmosphere

**设计哲学**：这不是一个网页，是一场可以点着走完的人生推演。界面服从叙事，叙事服从角色。

**氛围关键词**：深夜知识档案馆、屏幕蓝光、纸页翻动、心跳声、克制的终端噪点、被压住的呼吸。

**一句话定调**：像一本会在你犹豫时停下来等你的书，而不是一个等你填表的表单。

**核心取舍**：把「仪表盘」让位给「舞台」。所有元信息（属性、装备、命途）退到 HUD 与抽屉里，主视觉永远只讲一件事——**此刻谁在对你说话**。

**首页取舍（2026-09-12 视觉收敛）**：保留街机世界观，但不再把每个元素都做成发光控件。
第一视口只建立三层：品牌主张 → 已验证裂缝 → 自定义目标；蓝色只标记可交互与真实证据，
大面积表面使用低对比蓝黑和细边框，让真实经历与玩家输入成为视觉焦点。

**刻意偏离**：`web-design` skill 的 L3 scroll-story 模式（pin-scrub、滚动驱动时间线）面向纵向滚动叙事页；本产品是「场景切换型」叙事，滚动不是主要交互，因此不采用 scroll-story，替换为**幕转场 + 受击演出**两套编排。偏离理由记录于此，代码审计时按此判定。

---

## 2. Color Palette & Roles

所有颜色以 CSS 变量定义，代码中零硬编码 hex。

```css
:root {
  /* 基底：不是纯黑，是深夜蓝黑，带一点冷 */
  --gmv-ink-950: #04060B;      /* 页面底 */
  --gmv-ink-900: #070A11;      /* 舞台底 */
  --gmv-ink-800: #0B111D;      /* 面板 */
  --gmv-ink-700: #111A2A;      /* 浮起面板 */
  --gmv-ink-600: #1A2940;      /* 抽屉 / 高亮面板 */

  /* 知乎蓝：品牌锚点，只用于「系统 / 希望 / 可交互」 */
  --gmv-blue-500: #0084FF;
  --gmv-blue-400: #3D9BFF;
  --gmv-blue-300: #7CC0FF;
  --gmv-blue-100: #CFE8FF;

  /* 叙事情绪色 */
  --gmv-amber-400: #F5B841;    /* 遗物 / 高光 / 主动 */
  --gmv-jade-400: #3DD6A0;     /* 检定通过 / 恢复 */
  --gmv-rose-500: #FF4D6D;     /* 受击 / 检定失败 / SAN 危险 */
  --gmv-violet-400: #A78BFA;   /* 羁绊 / 人际关系 */

  /* 文本层级 */
  --gmv-text-100: #F4F7FB;     /* 标题 */
  --gmv-text-200: #C9D4E3;     /* 正文 */
  --gmv-text-400: #91A0B7;     /* 次要 */
  --gmv-text-600: #607088;     /* 禁用 / 水印 */

  /* RGB 辅助值（供 rgba() 使用） */
  --gmv-blue-500-rgb: 0 132 255;
  --gmv-rose-500-rgb: 255 77 109;
  --gmv-amber-400-rgb: 245 184 65;
  --gmv-jade-400-rgb: 61 214 160;
  --gmv-ink-800-rgb: 11 17 29;
  --gmv-ink-950-rgb: 4 6 11;
}
```

**角色立绘色板**（每个角色一组，用于 SVG 参数化着色）：

| 角色 | hair | skin | cloth | accent |
|---|---|---|---|---|
| 刘看山 | `#EAF3FF` | `#F6FAFF` | `#DCE9FB` | `#0084FF` |
| 室友 | `#2E3D57` | `#F0D8C4` | `#3A4A63` | `#7CC0FF` |
| 面试官 | `#1B2536` | `#E8CDB6` | `#F4F7FB` | `#FF4D6D` |
| 家人 | `#5A4A44` | `#EFD3BE` | `#8A7A72` | `#F5B841` |
| 知乎答主虚影 | `#7CC0FF` | `#7CC0FF` | `#0084FF` | `#CFE8FF` |

**色彩规则**：一屏之内，蓝（系统）、琥珀（遗物）、玫红（危险）三种强调色最多同时出现两种；第三种出现时必须伴随全屏演出（受击红闪或检定模态）。

---

## 3. Typography Rules

| 角色 | 字族 | 用途 |
|---|---|---|
| 叙事正文 | `"Noto Serif SC", "Songti SC", Georgia, serif` | 对白、旁白、内心独白——衬线体承担文学质感 |
| 界面 / HUD | `"Noto Sans SC", system-ui, "PingFang SC", sans-serif` | 属性、按钮、标签 |
| 数据 / 种子 | `"JetBrains Mono", ui-monospace, Consolas, monospace` | 数值、SEED、检定算式 |

```css
@import url('https://fonts.googleapis.com/css2?family=Noto+Sans+SC:wght@400;500;700;900&family=Noto+Serif+SC:wght@400;600;700&family=JetBrains+Mono:wght@400;700&display=swap');
```

**字号层级**（桌面 / 移动）：

| 级别 | 字号 | 行高 | 字距 | 用途 |
|---|---|---|---|---|
| 幕标题 | 40 / 28 | 1.15 | 0.04em | 每幕开场大字 |
| 对话框正文 | 19 / 17 | 1.9 | 0.02em | 叙事主体（≥ 17px） |
| 角色名 | 14 / 13 | 1.4 | 0.08em | 名牌 |
| HUD 数值 | 15 / 14 | 1.2 | 0.02em | 血条数字，等宽 |
| 标签 / 提示 | 11 / 11 | 1.5 | 0.12em | eyebrow、槽位号 |

**中文排版硬约束**：正文行高 ≥ 1.7、字距 0.02em、字号 ≥ 15px（叙事正文 17px 起）；中文字族在前、英文字族作 fallback。

**禁止**：把大段中文做成全大写感的宽字距标签、给单个词加粗变色来强调、在标题上方加无意义小标签、
在按钮文案后机械追加 `→`。短英文系统编号（如 `ARCHIVE / 01`）与独立方向图标可用，但必须承担定位或交互提示职责。

---

## 4. Component Stylings

### 4.1 HUD 血条 `StatBar`

```css
.gmv-bar {
  position: relative;
  height: 14px;
  transform: skewX(-14deg);           /* 斜切：游戏感的核心来源 */
  background: rgb(var(--gmv-ink-800-rgb) / 0.9);
  border: 1px solid rgb(255 255 255 / 0.08);
  overflow: hidden;
}
.gmv-bar__fill {
  height: 100%;
  transition: width 600ms cubic-bezier(0.22, 1, 0.36, 1);
}
.gmv-bar--san .gmv-bar__fill { background: linear-gradient(90deg, var(--gmv-rose-500), var(--gmv-amber-400) 55%, var(--gmv-jade-400)); }
.gmv-bar--skill .gmv-bar__fill { background: linear-gradient(90deg, var(--gmv-blue-500), var(--gmv-blue-300)); }
.gmv-bar--bond .gmv-bar__fill { background: linear-gradient(90deg, #6D4AFF, var(--gmv-violet-400)); }
.gmv-bar--hit { animation: gmv-bar-hit 320ms ease-out; }  /* 受击时抖动 + 白闪 */
```

状态：`default` / `hover`（数值放大 1.04）/ `hit`（受击抖动）/ `critical`（SAN < 30 时 fill 呼吸 + 外发光）。

### 4.2 对话框 `DialogueBox`

```css
.gmv-dialogue {
  background: linear-gradient(180deg, rgb(var(--gmv-ink-800-rgb) / 0.86), rgb(var(--gmv-ink-950) / 0.96));
  border-top: 1px solid rgb(var(--gmv-blue-500-rgb) / 0.28);
  backdrop-filter: blur(10px);        /* ≤ 14px，符合性能红线 */
}
.gmv-dialogue__name {
  letter-spacing: 0.08em;
  color: var(--gmv-blue-300);
}
.gmv-dialogue__caret {
  animation: gmv-caret 1s step-end infinite;
}
```

状态：`typing`（光标闪烁 + 「继续」隐藏）/ `ready`（▼ 呼吸 + 「继续」可点）/ `danger`（边框转玫红 + 正文轻微抖动）。

### 4.3 选项卡 `ChoiceCard`

- default：`border-white/10`，背景 `rgb(255 255 255 / 0.03)`
- hover：上浮 2px + `--mx/--my` 聚光（radial-gradient 跟随鼠标，rAF 节流）
- focus-visible：2px 知乎蓝环 + 2px offset
- disabled：`opacity .45` + `cursor-not-allowed`
- 高危选项：琥珀描边 + 左上角 `D20` 角标；稳妥选项：蓝色描边 + `无检定` 角标

### 4.4 按钮

| 变体 | default | hover | active | disabled |
|---|---|---|---|---|
| `arcade-btn` | 实心蓝 + 底部 6px 厚阴影 | 上浮 2px、阴影加深 | 下沉 4px、阴影压扁 | opacity .7 |
| `btn-ghost` | 透明 + 白 12% 描边 | 描边提亮至 25%、文字转白 | scale .98 | opacity .45 |
| 图标按钮 | 8×8 圆角、白 4% 底 | 蓝色描边 + 蓝字 | scale .96 | opacity .45 |

### 4.5 立绘 `Portrait`

- 说话者：`filter: none`，`transform: translateY(-6px)`，呼吸动画 3.2s
- 非说话者：`filter: saturate(0.35) brightness(0.6)`，`transform: translateY(0)`
- 入场：从 `translateY(24px) + opacity 0` 到就位，420ms `cubic-bezier(0.22,1,0.36,1)`
- 表情切换：眉/眼/嘴三组路径 180ms 交叉淡入

### 4.6 现实状态四轴 `AxisHUD`

常驻面板（推演舱抽屉内 + 对比页），**必须在 30 秒内让人看懂四轴**。

- 每条轴：图标（内联 SVG）+ 标签 + **三档定性语义**（充裕 / 一般 / 吃紧 / 见底）+ 数值
- 轨道：`-skew-x-[14deg]` 斜切，沿用 HUD 血条的游戏感语言
- **需求刻度**：琥珀色竖线标出「这条路需要 ≥ N」，让玩家看见自己在往哪边挪
- **越线才变红**：硬轴跌破需求刻度时整条转玫红 ——
  这是 §4.3 临界 Boss 在常态下的一致视觉预兆，不是等到 Boss 才第一次变色

状态：`default` / `drag`（拖拽中轨道提亮）/ `breached`（玫红）/ `unknown`（证据不足时置灰并标注）

### 4.7 证据应力条 `EvidenceStressBar`

证据网格里每条路线标题下方的水平条。

- 左端文案「证据薄弱」，右端「有据」，游标停在 `evidenceStrength`
- 中档文案必须带**分母**：`3 条样本 · 有据`
- 三档配色：`strong` 翡翠 / `partial` 琥珀 / `thin` 灰（`thin` 同时降低整块卡片的对比度）

**红线**：`thin` 路线的所有数值必须显示为「未知」，且卡片内明写
「这不是风险低，而是还不知道」。

### 4.8 临界点卡片 `CriticalPointCard`

**一次性全屏级事件**，信息密度极低、情绪冲击极高。

- 顶部：`临界点` 等宽标签 + 路线名
- 主体：一条**被红线切开的轴** —— 当前值刻度 vs 需求刻度，缺口段用玫红渐变
- 文案：一句叙事（`你不算错，只是撞上了现实的边界`）+ 一句动作（把某轴从 X 调到 Y）
- **诚实性约束**：只陈述差额与目标值，**不承诺「调整后必然成功」**；
  满足约束只代表这条路走得通

状态：仅终局出现一次，配 `flash-red` 转场；`spotlight` 形态额外加玫红外发光。

### 4.9 双牌对比 `SandwichView`

- 左右并排（`lg:flex-row`，窄屏纵向堆叠）—— 对比必须共时，眼睛要能一眼扫到差异
- 每张牌头：名称 + `可行数 / 总路线数` + 三轴当前值（等宽字体）
- **分歧路线高亮**：两张牌结论不同的路线加琥珀描边 + `两张牌结论不同` 标注 ——
  这是「看点」，不能让观众自己在两列里找不同
- 底部：一句总结点名是哪几条路线翻转了

**禁止**：把翻转表现成「红字变绿字」。翻转必须有明确视觉高潮（世界线接合 / 应力条跨越阈值）。

### 4.10 赛博契约 `CommitPicker`

- 每条候选：勾选钮 + 祈使句动作 + 时间盒 chip + 可验证信号
- 已认下：翡翠绿描边 + 到期徽标（`N 天后回访` / `今天到期`）
- 未登录：**不拦人**，只在顶部如实写明「认下之后不会被记住」
- 空候选时给出一条信息型文案，而不是留白

**禁止**：把承诺做成弹窗拦截或强制勾选。产品取向是「用损失感驱动登录，不用门槛拦人」。

### 4.11 四维结算 `ClarityRadar`

- 四维雷达图（SVG，四象限）+ 右侧逐条进度条与释义
- 维度名等宽、释义小字；结语用中文衬线体
- **禁止**：出现「得分」「评级」「星级」这类奖状语言 —— 它是能力画像，不是奖状

### 4.12 引擎状态条 `EngineStatusBar`

- 三行等宽指示灯：`AI ENGINE` / `ZHIHU EVIDENCE` / `WORLD ENGINE`
- `full` 模式翡翠描边，其余琥珀描边 + 把能力边界写在脸上
- **只显示在线 / 离线，永不显示密钥**

---

## 5. Layout Principles

**舞台优先，但交互绝不裁切**：推演页采用一个页面滚动轴——HUD / Stage（按视口钳制高度）/
DialogueBox（内容自然撑开）。禁止在根节点使用 `h-screen + overflow-hidden`，也禁止给选项区再套一层
不明显的内部滚动；较矮窗口、系统任务栏和长文案下，所有选项都必须能通过页面滚动到达。

```
┌─ HUD ────────────────────────────────────┐  64px
├─ Stage（场景 + 立绘）────────────────────┤  clamp(180px, 30vh, 320px)
│   立绘左位 · 立绘右位（中位用于独角戏）      │
├─ DialogueBox ────────────────────────────┤  内容自然撑开，跟随页面滚动
│   名牌 + 头像 + 正文 + 全部选项 / [继续 ▼]   │
└──────────────────────────────────────────┘
```

**首屏口径**：首页要让主输入与主按钮在常见桌面首屏内可见；推演页不强求把剧情和全部选项硬塞进
一屏，长内容允许下滑，但不能隐藏滚动能力，也不能出现页面与卡片双滚动。

**间距梯度**：4 / 8 / 12 / 16 / 24 / 32 / 48。舞台内元素只用 8/16/24；抽屉内用 12/16。

**容器**：舞台全宽无最大宽度（要满屏沉浸）；抽屉宽 380px（移动端 100%）。

**抽屉**：从右侧滑入，`translateX(100%) → 0`，280ms；遮罩 `rgb(5 7 13 / .6)` 点击关闭。

---

## 6. Depth & Elevation

| 层级 | 用途 | 阴影 |
|---|---|---|
| 0 | 场景背景 | 无 |
| 1 | 立绘 | `drop-shadow(0 18px 28px rgb(0 0 0 / .55))` |
| 2 | 对话框 / HUD | `0 -1px 0 rgb(255 255 255 / .05)` + `0 -24px 60px -32px rgb(0 0 0 / .9)` |
| 3 | 抽屉 / 模态 | `0 40px 90px -40px rgb(0 0 0 / .95)` |
| 4 | 演出层（红闪 / 飘字） | 无阴影，纯叠加 |

**规则**：不用「卡片 + 圆角 + 软灰阴影」这套 SaaS 默认组合。面板圆角统一 16px，但立绘、对话框、HUD 三者圆角不一致（分别是 0 / 20px / 12px），刻意让层级可被眼睛读出。

---

## 7. Animation & Interaction

**档位：L2（流畅交互）**。依赖：零第三方动画库，全部 CSS keyframes + 一处 rAF 节流鼠标聚光。

### 签名动效清单（skill 六类全覆盖）

| 类别 | 落点 | 实现 |
|---|---|---|
| Text — Hero | 首页大标题 mask reveal | `clip-path` 一次性 1.2s |
| Text — 章节 | 幕标题 BlurText 入场 | `filter: blur(8px) → 0` + `translateY`，480ms |
| Text — 正文 | 对白打字机 + 低 SAN 失真 | 逐字 interval；SAN < 30 时叠加 `gmv-text-unstable` |
| Animation — 元素级 | 选项卡聚光 + 受击飘字 | `--mx/--my` radial-gradient（rAF 节流）；飘字 `translateY(-56px)` 淡出 |
| Component — 交互构件 | 立绘位切换 + 抽屉 + 命途树 | 立绘呼吸/降饱和；抽屉 `translateX`；命运树 SVG 虚线流动 |
| Background — 氛围 | 场景视差 + 光晕呼吸 | 三层 `translate3d` 随鼠标微移（±8px），光晕 4.5s 呼吸 |

### 幕转场（替代 scroll-story）

- `glitch`：故障艺术，用于回合推进
- `flash-white`：白闪 160ms，用于高光时刻
- `flash-red`：全屏红闪 + 镜头抖动，用于 SAN 暴扣

### 性能红线（skill 同款）

- 移动元素禁用 `filter: blur()`；景深用 opacity + scale
- `backdrop-filter: blur()` ≤ 14px，不覆盖大面积滚动区
- `pointermove` 必须 rAF 节流
- 单页无 WebGL
- `prefers-reduced-motion: reduce` 下：所有 keyframes 降为 0.001ms，打字机直接整段呈现，视差关闭

---

## 8. Do's and Don'ts

**Do**
1. 主视觉永远留给「谁在说话」——立绘 + 对话框。
2. 属性变化必须同时有数字、颜色、动效三重反馈。
3. 每个角色必须有可辨识的剪影（发型 / 服装 / 配饰至少两处差异）。
4. 立绘表情必须跟随 SAN 与剧情 mood 变化。
5. 危险状态（SAN < 30）用全屏红晕 + 心跳脉动，而不是只把数字变红。
6. 每幕至少一处「伏笔回响」，让前一幕的选择在后续被提起。
7. 所有可交互元素必须有 hover + focus-visible 态。
8. 中文正文行高 ≥ 1.7、字号 ≥ 17px。

**Don't**
1. 不用「三栏 dashboard」布局承载叙事。
2. 不用等大圆角卡片网格装填内容。
3. 不给按钮文案追加 `→`（「继续」用 ▼ 指示符）。
4. 不用 Emoji 当图标；HUD 图标一律内联 SVG。
5. 不用全大写标签，不在标题上方加无意义 eyebrow。
6. 不给单个词变色来强调。
7. 不在滚动区域上盖大面积 blur。
8. 不用纯色块占位图片。
9. 不在非用户触发时叠加多个入场动画（一次编排 > 满屏 fadeInUp）。
10. 不让立绘与对话框在视觉上争夺注意力（对话框永远在最上层且更亮）。

---

## 9. Responsive Behavior

| 断点 | 布局 |
|---|---|
| ≥ 1024px | 立绘左右双位，对话框居中最大宽 920px，抽屉 380px |
| 768–1023px | 立绘左右位缩小 80%，对话框占满宽 |
| < 768px | 立绘只保留说话者（居中偏右），对话框贴底全宽，HUD 折成两行，抽屉 100% 宽 |

**触摸目标**：所有可点元素 ≥ 44×44px（HUD 图标按钮 40×40 但含 4px 外扩热区）。

**移动端降级**：视差关闭 → 静态渐变；立绘呼吸关闭；鼠标聚光关闭（`matchMedia('(hover: hover)')` 判定）。

**横向溢出**：< 600px 必须零横向滚动。

### 7.11 经验驱动世界蓝图：已落地的增量重构

> 本节记录一次**增量重构**的边界与状态。原则是**先加适配层，不一次删除旧实现**。
> **当前状态（2026-09-12）：裁剪版 P0-A～P0-H 全部落地**，新链由
> `tests/fullPersonalizedRun.test.ts`（§29 技术身份证）与 smoke 35 项断言端到端锁定。

#### 三条主轴的现状

| 主轴 | 位置 | 角色 |
|---|---|---|
| `DecisionSession` | `src/features/decision-session/` | **后台业务主轴**：问题 → 动态澄清 → prepare-world → 蓝图 → 实验 |
| Play（推演舱） | `src/app/play/page.tsx` | **前台游戏主轴**：多幕剧本 + 裁决 + 遗物；`/play?session=<id>` 消费世界蓝图 |
| 经验引擎 | `src/features/experience/` | **新链**：检索 → 逐字片段 → 经历 → 动态路径 → 差异对照 |
| 世界编译 | `src/features/game-world/` | **经验 → 游戏**：`compileWorld`（纯函数）+ DM 上下文切片 + 解锁注入 |
| 首页 | `src/app/page.tsx` | 双入口：投币进游戏 / 先看现实对照 |

#### 已落地的 Experience Engine（P0-A～P0-H）

把「真实知乎经历」从**证据展示**升级为**可编译进游戏的领域模型**：

```text
ProblemFrame（分离现实事实与叙事推断）            frame.ts
  → 动态澄清 0～2 条（说过的不重复问）            clarification.ts
  → 多意图检索（similar / alternative / counterexample，预算 3–4、并发 ≤2）  queryPlan.ts / retrieve.ts
  → 检索结果落盘缓存（TTL 12h，进程重启不烧配额）  searchCache.ts
  → ExperienceFact（逐字片段；AI 只有提议权）      extract.ts / validate.ts
  → ExperienceCase（把同一来源的片段拼成「一个人的经历」）  cases.ts
  → ExperiencePath（模型只分组不创作；验证不过回落 legacy 聚类）  pathSynthesis.ts / legacyAdapter.ts
  → UserDifference（数值算术 / 逐字命中，否则 unknown）  compare.ts
  → WorldBlueprint（纯函数编译：进入世界 → 体验代价 → 遇见反例 → 终局反思）  game-world/compileWorld.ts
  → /play?session=<id> 消费 Blueprint（LOAD_WORLD_BLUEPRINT，与 memory 同模式异步装载）
  → DmTurnInput.worldContext（本幕冲突 + 可引用原文 + 差异 + 现实边界；sourceFacts 转引用片段，整局不再逐幕搜索）
  → Experience Unlock Choice（选项 <3 才注入、无检定、带【经验解锁】徽标可回溯原文）
```

每一步的**纪律落点在代码里**，不靠约定：

| 纪律 | 执行点 | 锁定测试 |
|---|---|---|
| AI 改写一个字就拒绝 | `validate.ts` 逐字子串校验，无模糊修复 | `experienceQuoteIntegrity.test.ts` |
| 不重复问已说过的 | 澄清候选完全来自 `frame.unknowns` | `dynamicClarification.test.ts` |
| 不引入成功率 / 匹配度 | `pathSynthesis.ts` 禁词表，命中即整条 fallback | `pathSynthesis.test.ts` |
| 差异不能明确比较就是 unknown | `compare.ts` 只做数值算术与逐字命中 | `userDifference.test.ts` |
| 没有事实不伪造世界 | `compileWorld.ts` 空内容留空 + 冲突文案如实说「没有找到」 | `worldBlueprint.test.ts` |
| 旧路径零回归 | 无 session 参数时 `/play` 行为不变；旧固定三问仍是旧会话的 fallback | smoke 35 项断言 |

#### 迁移期纪律（不可违反）

1. **不做全仓重构**，不为「架构更漂亮」搬动无关文件；
2. **不删除**任何现有离线 fallback、黄金案例、AI DM fallback、匿名身份 cookie、OAuth、memory、challenge、boss；
3. **不新增运行时依赖**；
4. **不伪造** `sourceUrl` / 作者 / 原文 / 点赞数 —— AI 只能引用，不能发明；
5. **不引入**「成功率 / 匹配度 / 推荐分」等伪精确数值；
6. **不把游戏结局表述为现实预测**；
7. 优先**增加适配层**，旧实现暂时保留并标注迁移方向。

#### 迁移期保留的旧实现

| 模块 | 状态 |
|---|---|
| `core/evidence/*`（EvidenceMesh / 证据网格） | **保留**：仍为推演舱证据展示的来源 |
| `data/prebuiltScenarios.ts`（预置剧本） | **保留**：离线演示的稳定路径 |
| `features/decision-session/routes.ts`（固定走法表） | **保留**：作为动态路径的 legacy fallback |
| `core/decision/verdict.ts`（四轴裁决） | **保留**：游戏机制本身，本轮不动 |
| 所有 `fallbackChain` / offline 分支 | **保留**：去掉它们会让站点在无 key 时不可用 |
