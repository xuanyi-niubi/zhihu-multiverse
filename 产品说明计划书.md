# 知乎平行宇宙 · 产品说明计划书

> 知乎黑客松 2026 · 校园新锐季
> 主赛道：🎮 跨次元游乐场 ｜ 副赛道：📚 知识炼金场
> 一句话：用 3 分钟的平行推演，跨越现实中 3 年的人生迷茫。

---

## 一、项目定位

**知乎平行宇宙（Zhihu Multiverse）** 是一个把知乎真实问答转化为可玩人生的**文字 AVG 推演机**。

玩家输入一句当下的迷茫（"大三法学，想转计算机，但怕脱产找不到工作"），系统检索知乎站内真实讨论，由 AI 地下城主生成多幕关卡（预置剧本 4 幕；AI 自由推演按张力预算 4–8 幕）；玩家在每一幕做一个抉择，高风险选项要过 D20 检定；走到终局，系统把这一局踩过的坑沉淀成一份《专属避坑指南》。

**它不是问答壳子。** 区别在于三点：

| | 通用 AI 聊天工具 | 知乎平行宇宙 |
|---|---|---|
| 输入 | 一句问题 | 一句迷茫 |
| 输出 | 一段建议文本 | 一条可走完的人生路径 + 结局 |
| 知识来源 | 模型参数里的模糊记忆 | 知乎站内高权威回答，逐条可溯源 |
| 体验 | 单向咨询 | 概率博弈 + 卡牌构筑 + 可复盘 |

**一句话差异**：通用模型告诉你"转码要做什么"，我们让你先**在虚拟宇宙里试穿一次转码失败的后果**，再决定要不要下这个注。

---

## 二、痛点与破局

### 痛点

1. **知乎沉了 15 年最扎实的踩坑长文，但"收藏即吃灰"。** 信息密度极高，可读性极低；一篇万字长文里的关键决策点，藏在第 7 段的括号里。
2. **通用大模型能对话，但缺乏真实社区语料背书。** 问"双非能不能进大厂"，它会给你一段四平八稳的鼓励，而不是一个 2019 年真的从双非进了字节、并在评论区补充了三年后现状的人。
3. **真正的问题是"沉没成本不可感知"。** 学生不缺信息，缺的是**对不同选择背后代价的直观感受**。知道"转码很难"和**亲身经历一次转码失败**，是两种认知。

### 破局

把知识消费从"读"改成"走"。

- 静态长文还原成**动态关卡**：矛盾点变成选项，风险等级变成检定难度。
- 知乎答主的金句变成**局内装备**：检定成功掉落一张写着他原话的遗物卡。
- 走过的岔路变成**命运树**：终局可视化你这四步怎么走的、哪一步偏了。

---

## 三、核心机制

### 3.1 AI 动态地下城主

两级剧本，兼顾**演示稳定性**与**万物皆可推演**：

- **精调剧本**：命中关键词（转码 / 考研 / 秋招 等）时走离线精调脚本，零延迟、零失败风险。
- **AI 自由推演**：输入任意生僻方向（"农业工程跨界人形机器人"），AI DM 现场检索知乎站内讨论、抽取矛盾点、动态生成关卡。

### 3.2 D20 属性检定与风险博弈

告别"选 A 还是选 B"的问卷模式，引入 TRPG 式概率博弈：

- **稳妥选项**：无检定，收益有限但可控（如"按部就班刷题"）。
- **高危选项**：必须过检定，成功爆高光、失败扣心智（如"通宵手撕分布式项目"）。

三条属性线：**心智 SAN**（归零即终局）、**专业力 skill**、**社区羁绊 bond**。

### 3.3 知乎内容装备化

知乎高赞回答具象化为局内卡牌，3 个装备槽：

- **被动卡**：如【大 V 答主反卷箴言】，持续降低 SAN 损耗。
- **主动卡**：如【盐选速记秘卷】，消耗品，为下一次检定提供一次性大幅加成。

### 3.4 魂系异步共鸣

选项旁呈现社区数据（"52% 的同路人在此处犹豫过"），让单机文字游戏带上知识社区特有的群体感。

### 3.5 跨周期记忆：从"一次性玩具"到"记得你的东西"

这是本产品相对同类文字游戏**最关键的一条护城河**。玩家玩了一次看完结局，第二天为什么还来？

三层设计：

1. **既视感开场**：第二次打开时，AI 以知乎老友的口吻点出你上一局在哪里跌倒——"上一次你在第 3 幕因为大厂算法连环追问心态崩了，这次改不改做法？"
2. **决策画像沉淀**：系统从你的历次选择里提炼标签（硬刚型 / 求稳型 / 借力型 / 独行型 / 同辈焦虑 / 后程崩盘），并把这些标签注入后续每一局的生成 Prompt。
3. **前世遗念卡牌**：上一局你写下的那句反思（"别盲目背八股，先做出一个能讲清楚的项目"），直接变成下一局的开局卡牌【前世的避坑顿悟】，为专业力检定提供永久加成——**走得越远，那一世的教训越值钱**。

#### 记忆为什么必须绑定知乎账号

这是本产品接入知乎 OAuth **真正的产品理由**，而不是为了接接口而接接口：

| | 游客 | 已登录 |
|---|---|---|
| 能不能玩 | ✅ 能，完整推演 | ✅ 能 |
| 这一局会被记住吗 | ❌ 不会 | ✅ 存入你的账号 |
| 跨设备可见 | — | ✅ 换电脑、换浏览器都在 |
| 下一局有前世遗念吗 | ❌ 没有 | ✅ 有 |

设计取向是**用损失感驱动登录，而不是用门槛拦人**：
游客不会被弹窗拦住，可以完整体验一遍；但产品会在两个决策点**如实告知代价**——
开局前告诉你"这一局不会被记住"，结算时告诉你"下一局不会有前世遗念带你少走弯路"。
愿意留下的人，自然去连接账号。

**身份识别用知乎的 `url_token`（个人主页永久标识），不用昵称。** 昵称可以改、可以重复，
拿它当主键会让两个同名用户读到彼此的记忆；取不到 `url_token` 时宁可不保存，也不退化。

服务端按账号隔离存储（原子写入，单账号保留最近 20 局历史）。未登录的写入会被明确拒绝并返回
`authenticated: false`——这是正常状态而非错误，前端据此隐藏记忆相关 UI。

### 3.6 种子裂变

每一局生成唯一宇宙种子（`SEED-2026-XXXXX`）。**同一颗种子 = 同一组骰面**——终局生成的挑战链接，别人打开后面对的是完全一致的事件序列，成绩可比。

这依赖一套确定性设计：随机源由 `seed + checkId + turnIndex` 派生，刷新页面结果不变，任何一局都可复盘。

---

## 四、技术架构

### 4.1 技术栈

| 层 | 选型 |
|---|---|
| 框架 | Next.js 14.2（App Router）+ React 18 |
| 语言 | TypeScript 5.5（strict） |
| 样式 | Tailwind CSS 3.4，全部颜色走 CSS 变量 |
| 测试 | Vitest 2.1，**58 个文件 984 个用例**（唯一口径见 `scripts/test-stats.json`，由 `npm run test:stats` 生成） |
| 运行时依赖 | 仅 React / Next，**零动画库、零状态库、零图片素材** |

### 4.2 AI DM 的五层容错防御

**核心工程主张：仅靠 Prompt 无法保证 100% 输出合法 JSON。** 谁承诺能保证都不成立。所以本项目把"不崩"放在代码层——只有第 1 层依赖模型自觉，其余四层全是确定性代码：

| 层 | 实现 | 职责 |
|---|---|---|
| ① Prompt 约束 | `core/dm/prompt.ts` | 六节系统 Prompt、JSON Schema 单一事实源、Few-shot、修复 Prompt |
| ② JSON 模式传输 | `core/dm/provider.ts` | `response_format: json_object`、AbortController 超时、失败结构化（不抛） |
| ③ 容错解析 | `core/dm/parse.ts` | strict → 剥围栏 → 括号配平 → 损坏修复 → 补齐截断 → 单引号归一 |
| ④ 校验与钳制 | `core/dm/validate.ts` | DC 收敛、属性钳制、稳妥选项摘 check、缺失败分支合成、非法遗物丢弃 |
| ⑤ 编排兜底 | `core/dm/generate.ts` | 重试一轮 → 离线剧本 → 硬编码最小关卡 |

**契约**：`POST /api/dm` **永远返回 HTTP 200 与一个合法关卡**。前端不存在错误分支。响应头 `x-dm-source` 标明来源（`model` / `model-repaired` / `fallback`）。

解析层实测能处理的畸形形态：markdown 围栏、尾随逗号、全角标点、`NaN`/`Infinity` 字面量、单引号 JSON、被 `max_tokens` 截断在字符串中间、字符串内含花括号、8000 字噪声前缀、零宽字符污染。**27 类语料逐条断言永不抛异常**（65 个用例）。

### 4.3 确定性设计

同一份输入必须得到同一个结果，否则"种子挑战"不成立。

- **随机源**：`mulberry32`，由 `seed + checkId + turnIndex` 派生。
- **判定优先级**：天然 1 必败、天然 20 必成，高于数值比较。
- **数值钳制**：属性 0–100、DC 1–30、单项修正 ±20、总修正 ±60。
- **结算顺序**：先乘出身易伤 → 再乘遗物减伤 → 最后四舍五入，顺序固定且被单测锁死。

类型层用 branded types（`StatValue` / `D20Difficulty` / `D20Modifier`）让越界值难以构造；`ZhihuRelic-D20-Spec.ts` 是一份 579 行纯 Spec 文件，含 12 条编号不变量。

### 4.4 知乎生态接入

| 官方能力 | 落地方式 |
|---|---|
| **知乎搜索** | AI DM 每一幕按"幕序 + 冲突主题"检索差异化语料，注入生成 Prompt；前端溯源角标显示真实答主 / 赞同数 / 原回答链接。LRU + TTL 缓存保护 1000 次/天配额。 |
| **知乎热榜** | 首页"今日热榜"模块，为"不知道纠结什么"的玩家提供真实议题入口，一键填入推演目标。 |
| **知乎故事** | 故事品类接入客户端（路径可配置，失败自动降级不影响主链路）。 |
| **知乎 OAuth** | `/oauth` 账号接入面板，可读取登录账号的公开资料、创作、关注与收藏。 |
| **刘看山 IP** | 参战导师角色；SAN 危急时可消耗 30 羁绊触发"呼叫大 V 救场"强行锁血。 |

**OAuth 实现要点**：三类凭证（App ID / OAuth App Key / Access Secret）严格分离，串位会被 `credentialWarnings` 主动拦下；所有诊断只输出长度与 sha256 前缀，明文永不进入响应、页面或日志；OAuth Token 只存 Node 进程内存，不落盘。

---

## 五、与知乎生态的契合度

**这是产品成立的前提，不是附加项。**

1. **内容是原料而非背景**：没有知乎的真实语料，AI DM 生成的就是通用鸡汤；有了站内高权威回答，"法学能不能考知产岗"这种细节才立得住。
2. **每一句金句可溯源**：溯源角标直接跳原回答，答主、赞同数都在。这让产品成为知乎内容的**新入口**，而不是内容的搬运方。
3. **沉淀的是社区数据**：玩家在推演中暴露的真实焦虑与决策偏好，结构化沉淀为画像。若与主站打通，知乎可据此推送该用户真正需要的专栏、Live 与咨询——**从"猜他喜欢什么"到"知道他卡在哪"**。
4. **天然可传播**：种子挑战机制让一局推演变成可分享的社交货币，回流站内想法。

---

## 六、完成度说明

### 已实现

- 多幕推演完整闭环（选择 → 检定 → 结算 → 终局报告）
- AI DM 五层容错 + 离线精调剧本兜底
- D20 检定引擎（含天然 1/20 优先级、修正钳制）
- 三类出身流派、遗物系统（3 槽位、主被动、SAN 减伤上限）
- 命运树可视化、遗物栏抽屉、斜切血条 HUD
- 参数化 SVG 立绘引擎（5 造型 × 7 表情）+ 官方刘看山动态素材
- 分层视差场景舞台（7 场景，rAF 节流）
- 叙事节拍系统（离线剧本与 AI 关卡共用同一套演出管线）
- 跨周期记忆：**服务端按知乎账号隔离存储**，含既视感开场、决策画像、前世遗念卡牌；
  游客可完整体验但记忆不保留
- 种子挑战裂变链路（`/challenge` 落地页）
- 知乎 OAuth 账号接入 + 五项用户接口验收
- 知乎搜索 / 热榜 / 故事接入
- 终局《专属避坑指南》生成（模型 + 确定性模板双路径）
- **984 个自动化测试全部通过**（`scripts/test-stats.json` 为唯一口径）；`tsc --noEmit` 与生产构建均通过
- **已部署上线**：https://zhihu.xuanyi888.cloud:8443 （Docker + Nginx，独立子域名与证书；443 亦可访问）

### 已知边界（如实说明）

- AI DM 单关耗时受模型输出长度主导；3 分钟演示走离线精调剧本（零延迟）。
- 首次使用需本人完成一次知乎授权，之后记忆才跟账号走。
- OAuth Token 存进程内存，服务重启需重新授权；因此**必须单实例部署**（已锁定）。
- 协议侧已知缺口：回调可能不返回 `state`，暂无 PKCE / refresh token / 撤销与解绑。**这是联调能力，不是可直接上线的生产登录方案。**
- 账号记忆按单账号保留最近 20 局；服务端存储在容器卷内，未做多副本同步。
- 遗物套装共鸣、双骰检定、知识图谱复盘尚未实现。

---

## 七、演示动线（3 分钟）

| 时间 | 内容 |
|---|---|
| 0:00–0:25 | 痛点：知乎沉了最好的踩坑长文，但收藏即吃灰；通用模型给不了有据可依的答案 |
| 0:25–0:45 | 首页点击「连接知乎账号」→ 本人完成授权 → 回来时首页显示「宇宙记得你走过的 N 局」。**先登录，是为后面的记忆闭环铺垫** |
| 0:45–1:50 | 输入"大三法学，想转计算机但怕脱产"→ 走完两幕 → 展示溯源角标、D20 检定、遗物掉落、命运树 |
| 1:50–2:25 | 触发 SAN 危急 → 消耗羁绊呼叫看山救场 → 进入终局 → 展示《专属避坑指南》与决策画像 |
| 2:25–3:00 | 写下遗念 → 复制挑战链接 → **重开一局，展示 AI 以老友口吻点出你上一局的挫折**（记忆闭环）→ 收束到生态价值 |

> 如果现场网络不便完成真实授权，可以先展示「游客 → 登录」的引导界面：
> 首页与结算页都会如实说明"这一局不会被记住"，这本身就是产品设计的一部分。
> 记忆闭环则可提前用已授权的账号演示。

