# 知乎平行宇宙 · 产品说明计划书

> 知乎黑客松 2026 · 校园新锐季  
> 主赛道：🎮 跨次元游乐场 ｜ 关联方向：📚 知识炼金场  
> 在线体验：https://zhihu.xuanyi888.cloud:8443

## 一句话

**把别人真实走过的人生，变成你原本没看见的行动。**

知乎平行宇宙是一款由知乎真实人生经验驱动的互动人生实验。玩家输入一句正在经历的困惑，系统不会直接给建议，而是先厘清会改变结论的条件，主动寻找相似经历、另一种走法和反例，再把这些经历编译成一场约 3 分钟的互动人生实验。

> **真实的人走过的路，是这个世界的规则。**  
> **一条真实经历，会直接解锁你原本没有想到的行动。**

---

## 一、我们解决什么问题

学生和职场新人面对“要不要转专业”“要不要脱产转行”“该不该参加比赛”时，通常不缺观点，缺的是三样东西：

1. **别人在什么条件下做了什么。** 一句“可以转”没有意义，时间、资金、基础和退路才决定这条路是否适用。
2. **这条路真实付出了什么代价。** 成功经验更容易被看见，失败、后悔和中途退出却往往决定决策质量。
3. **哪些地方仍然不知道。** 通用 AI 容易为了完整而补齐答案，但有限的个人经历不能被包装成成功概率。

知乎沉淀了大量带处境、过程、代价与复盘的真人长文，但用户要在许多回答中自行寻找与自己相似的人、识别分歧，再把结论换算到自己的条件上。传统搜索给链接，聊天模型给建议；两者都没有让用户亲自体验“条件变化会怎样改变世界线”。

我们的解法不是再生成一份职业规划报告，而是把知识消费从“读答案”改成“走一遍”：

```text
真实困惑
  → 只追问会改变结论的条件
  → 检索相似经历 / 另一种走法 / 反例
  → 逐字提取可追溯经验
  → 编译成三幕平行世界
  → 玩家改变条件、承担代价、触发世界线分叉
  → 形成一个可验证的现实行动
```

---

## 二、为什么必须是知乎

知乎内容不是背景资料，也不是装饰性的“引用来源”，而是游戏世界规则的数据源。

### 2.1 真人经历提供规则边界

一条回答里明确写出的起点、行动、持续时间、代价和结果，会被结构化为经验片段。原文没有写出的信息保持未知，不通过推测补齐。

### 2.2 分歧比共识更重要

系统的检索计划强制包含三种意图：

- **相似经历**：与用户处境接近的人如何行动；
- **另一种走法**：面对同一问题，选择不同路径的人；
- **反例**：失败、后悔、踩坑或中途退出的人。

反例不是偶然搜到的，而是主动检索的。如果只寻找支持用户原想法的内容，产品就会退化成一个讨好的搜索框。

### 2.3 每条关键经验都可回到原回答

真实来源保留答主、原文片段、赞同数、抓取时间与原回答链接。用户可以从游戏中的一句话回到知乎完整讨论，产品因此成为知乎优质内容的新入口，而不是内容搬运终点。

项目已通过官方搜索能力同步并落盘 **60 条真实来源**，覆盖 5 个演示 Case；运行时支持实时搜索、来源快照和离线兜底。

---

## 三、AI、知乎、游戏引擎和玩家各自做什么

| 参与者 | 职责 | 明确不能做的事 |
|---|---|---|
| **知乎** | 提供真人经历、真实路径、真实代价与分歧 | 不把单条轶事包装成普遍规律 |
| **AI** | 理解问题、提出抽取候选、归纳经历、生成场景与叙事 | 不修改原文证据，不决定胜负 |
| **游戏引擎** | 根据玩家条件和路径要求执行确定性规则 | 不用随机数决定人生真相 |
| **玩家** | 选择行动、改变资源和条件、决定愿意承担什么 | 不被系统替代作出人生决定 |

四条不可违反的原则：

1. **先有证据，再有想象。** AI 可以自由叙事，但无证据内容不能进入关键裁决。
2. **AI 不决定胜负。** 路径能否成立来自确定性规则，而不是模型的一句话。
3. **未知就是未知。** 无据字段保持 `unknown`，不生成匹配度或成功率。
4. **随机决定遭遇，不决定真相。** 骰子可以决定遇到什么，但不能改变现实边界。

---

## 四、核心体验

### 4.1 输入一个真实困惑

例如：

> 大三法学，想转计算机，但怕脱产以后找不到工作。

系统先构建问题框定，区分“用户明确说过的事实”和“系统解析出的待确认信息”。

### 4.2 只问真正会改变后续的问题

系统根据缺失变量动态生成 0～2 个澄清问题。已经说过的信息不会重复问；每一问都会说明“回答后会改变什么”。用户也可以跳过，跳过的地方会保留为“待验证”，而不是由 AI 猜测。

### 4.3 看见真实的人怎样走

系统按相似经历、替代路径和反例进行多意图检索，把回答中的事实片段聚合成若干条动态路径，并展示：

- 他们当时的条件；
- 采取了什么行动；
- 付出了什么代价；
- 与用户相同、不同和仍未知的地方；
- 支持证据与反例之间的分歧。

这里不输出“匹配度 87%”或“成功率 72%”。个体经验不足以支持这种数字。

### 4.4 生成属于用户的三幕世界

经过澄清的处境与真实经验被编译为固定结构的世界蓝图：

1. **进入世界**：建立这个平行身份与目标；
2. **体验代价**：让资源消耗和现实约束进入选择；
3. **遇见反例**：强制让玩家面对失败、后悔或另一种走法；
4. **终局反思**：不替玩家下结论，而是暴露决定世界线的变量。

### 4.5 真实经验解锁原本不存在的选择

知乎经验不是剧情旁边的一张引用卡。满足触发条件时，一条真实经历会解锁一个此前不存在的选项，并显示【经验解锁】标记。玩家可以采用它，也可以拒绝它；但这条选择确实因为真实的人走过，才进入了当前世界。

---

## 五、真实性与可信度设计

### 5.1 AI 只有提议权，没有改写权

经验抽取采用“模型提议、代码裁决”：AI 提议原文片段，校验器要求 `exactQuote` 必须是来源原文的逐字子串。多一个字、少一个字或改一个字，候选都会被丢弃。

### 5.2 模型只归组，不创作人生经历

模型可以把多个片段归成一段经历或一条路径，但组成它的事实必须引用已验证片段。出现裁决式禁词或无法回溯时，整条模型结果会降级到确定性聚类。

### 5.3 未知是一等公民

系统只区分：

- 与你相同；
- 与你不同；
- 还不知道。

“还不知道”不会被换算成零，也不会被隐藏，更不会被补成一个看似精确的数字。

### 5.4 运行模式如实显示

| 模式 | 能力 |
|---|---|
| **FULL** | AI 动态理解 + 知乎实时检索 + 动态叙事 + 确定性裁决 |
| **AI** | AI 动态叙事 + 已验证快照或离线规则 |
| **DEMO** | 精调场景 + 已落盘真实快照 + 确定性引擎 |

系统按可用能力自动降级，不因外部接口故障中断一局，也不会把离线内容伪装成实时结果。

---

## 六、技术实现

### 6.1 技术栈

- Next.js 14.2（App Router）
- React 18.3
- TypeScript 5.5 strict
- Tailwind CSS 3.4
- Vitest 2.1
- 除 React / Next 外无第三方运行时依赖

### 6.2 经验引擎

```text
ProblemFrame
  → SearchPlan（3 种强制意图，3～4 次请求预算）
  → ExperienceFact（逐字片段）
  → ExperienceCase（事实聚合）
  → ExperiencePath（动态路径）
  → UserDifference（相同 / 不同 / 未知）
  → WorldBlueprint（三幕 + 经验卡/经验解锁）
```

搜索结果有持久化缓存，既保护官方 **1000 次/天**的知乎搜索配额，也保证同一问题整局只检索一次。

### 6.3 AI DM 五层容错

1. Prompt 与结构约束；
2. JSON 模式传输与超时控制；
3. 容错解析；
4. 代码校验与数值钳制；
5. 模型重试 → 离线剧本 → 最小合法关卡。

模型失败不会让前端白屏；所有降级都有明确来源标记。

### 6.4 确定性因果引擎（Legacy 路径）

同一 Manifest 会生成相同的事件计划、骰面和状态变化。随机数只决定遭遇与事件顺序，硬条件、临界点和世界线是否成立由纯函数规则计算，因此结果可复盘、可测试。**该引擎现在只服务旧推演路径（`/play?goal=` 与旧挑战链接）；当前主链不使用骰面与属性，它的确定性来自纯函数世界编译。**

### 6.5 数据与身份边界

- 已验证来源才允许展示赞同数；
- 密钥只存在服务端，不进入 URL、页面和日志；
- 游客不登录也能完成核心体验；
- OAuth 用于可选的跨局记忆，不作为进入游戏的门槛；
- OAuth 会话当前为单实例内存存储，属于比赛版能力，不宣称为生产级账号系统。

---

## 七、完成度

### 已实现

- 自定义困惑 → 问题框定 → 0～2 条动态澄清；
- 相似经历 / 替代路径 / 反例三类强制检索；
- 原文逐字经验抽取与来源回溯；
- 经历聚合、动态路径、用户差异计算；
- 三幕世界蓝图与 `/play?session=` 游戏接入；
- 真实经验解锁新选项；
- 未知锁（只有现实能回答的事永远锁着）与终局现实支线；
- **（Legacy 路径）** 确定性运行引擎、临界点、世界线分叉、多结局 —— 代码保留并服务旧推演路径，当前主链不使用；
- AI DM 五层容错与完整离线兜底；
- 来源快照、实时检索缓存和运行模式显示；
- 决策实验、七天回访及账号记忆基础能力；
- Docker + Nginx 公网部署；
- **测试规模以 `scripts/test-stats.json` 为准**（由 `npm run test:stats` 真跑产出，文档不写死数字）；
- 类型检查、生产构建与 HTTP 冒烟统一由 `npm run verify` 串起，结果同样以脚本输出为准。

### 比赛版边界

- 实时检索和动态生成受第三方接口延迟与配额影响，因此保留已验证快照和离线演示路径；
- 单条人生经历只能说明“这个人经历了什么”，不能生成群体成功率；
- OAuth Token 仅存单实例进程内存，服务重启后需要重新授权；
- 线上入口目前使用 `:8443`，核心游客体验可用，OAuth 回调仍需按平台实际支持情况验证；
- 不把未验证的接口猜测（曾经的知乎故事端点）、复杂社交系统、排行榜或知识图谱列为当前完成能力；故事类端点的代码已随本次去伪删除。

---

## 八、与知乎生态的价值

1. **让长回答从被收藏变成被体验。** 用户不必先读完大量长文，先进入由真实经验形成的情境，再回到最相关的原回答。
2. **让反例进入推荐逻辑。** 系统不只放大认同，而是主动寻找失败与后悔，降低信息茧房和确认偏误。
3. **为优质内容创造新的分发入口。** 每条关键经验均可跳回原回答，游戏行为带来对原内容的深度消费。
4. **沉淀结构化的真实问题。** 用户暴露的不是泛兴趣标签，而是“在哪个选择、哪项条件、哪个未知上卡住”，未来可用于更精准的知乎内容推荐与回访。
5. **形成可持续闭环。** 一局最后不是一句建议，而是一个可验证的小实验；七天后回收结果，让经验从内容转化为行动数据。

---

## 九、操作说明与仓库

1. 打开在线体验；
2. 在首页输入当前最纠结的问题；
3. 回答或跳过动态澄清；
4. 查看真人路径、证据分歧和未知；
5. 点击“生成我的世界”；
6. 世界就绪后点击“进入这个世界”；
7. 在三幕推演中拿到借来的经验（Experience Card），让真实经历解锁新行动；
8. 在终局确认「任何人的经验都替不了的那个问题」，认下一条未来七天的现实支线。

游客无需登录即可完成核心体验。账号连接只用于保存跨局记忆。

- 代码仓库：https://github.com/xuanyi-niubi/zhihu-multiverse
- 在线体验：https://zhihu.xuanyi888.cloud:8443
